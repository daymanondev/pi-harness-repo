# US-007 kicker slice 1 — research `to-issues` + skeleton SKILL.md

## Status

implemented

## Lane

normal

## Product Contract

First vertical slice of the `harness-project-kicker` skill (umbrella intake

# 9). Read mattpocock `to-issues` to learn its vertical-slice decomposition

method, then write the **skeleton** of `skills/harness-project-kicker/SKILL.md`
(frontmatter + section headers + leading word "slice"), applying the
`writing-great-skills` principles (user-invoked, progressive disclosure). No
step bodies yet — those are US-008.

## Evidence

- Read `mattpocock/skills` `engineering/to-issues/SKILL.md` (cloned at
  `/tmp/pi-github-repos/mattpocock/skills@main`). Key method learned: **tracer-
  bullet vertical slices** — each slice cuts through all layers end-to-end, not
  a horizontal slab of one layer; quiz user on granularity/dependencies; issue
  template carries Acceptance criteria + Blocked by.
- Created `skills/harness-project-kicker/SKILL.md` skeleton: frontmatter
  (`disable-model-invocation: true` → user-invoked, zero context load), one-line
  purpose,

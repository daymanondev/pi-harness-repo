# US-009 kicker slice 3 — dogfood the kicker on a sample requirement

## Status

planned

## Lane

normal

## Product Contract

Third slice of `harness-project-kicker` (umbrella intake #9). Validate the
skill by running it on a sample requirement: confirm it records a
`new_initiative` intake, writes a roadmap (not a monolithic spec), and
decomposes into ≥2 small slices without starting to code. Fix rough edges
found during the dogfood run.

## Evidence

To be added when this slice is implemented.
